PORT=3008
DATABASE_URL="postgresql://user:password@localhost:5440/notificaciones_db?schema=public"
RABBITMQ_URI=amqp://user:password@localhost:5672
# T-24: orígenes permitidos para el handshake WebSocket (Socket.IO). Misma variable
# y formato (lista separada por comas) que el CORS HTTP de los main.ts. En producción
# debe contener el/los dominio(s) https reales de la PWA, p.ej.:
# CORS_ORIGIN=https://app.tudominio.com
CORS_ORIGIN=http://localhost:4200,http://localhost:5173
